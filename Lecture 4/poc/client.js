let lib = require("./lib");

//  or 
// let {happyLibrary, myFn} = require("./lib");

// console.log(myFn());

lib.happyLibrary();
// lib.myProperty
// we have to console this.
console.log(lib.myProperty);
